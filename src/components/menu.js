// let menucontainer = document.querySelector(".menu");
// let menu = async () => {
//   let data = await fetch("http://localhost:3000/menu");
//   let res = await data.json();
//   res.map((menu) => {
//     let pic = `<div><p class="text-center h-[40px] text-sm text-gray-700">${menu.category}</p></div><div></div>`;
//     menucontainer?.insertAdjacentHTML("beforeend", pic);
//   });
// };
// export default menu;
